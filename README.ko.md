[English](README.md) | 한국어

# dd

> **"클립보드 좀 봐줘" 그만. 그냥 `/dd`.**

`dd`는 지금 클립보드에 있는 걸 — 스크린샷, 디자인 레퍼런스, 에러 로그, 코드 조각 — Claude Code에 바로 떨궈서 작업합니다. 붙여넣기도, "방금 복사한 이미지 보고" 같은 설명도 필요 없어요. 원본은 로컬 파일에 남고, 대화에는 짧은 요약만 들어갑니다.

[빠른 시작](#빠른-시작) • [왜 dd인가](#왜-dd인가) • [작동 방식](#작동-방식) • [주요 기능](#주요-기능) • [명령어](#명령어) • [요구사항](#요구사항)

---

## 빠른 시작

### 1. 마켓플레이스 추가

```
/plugin marketplace add https://github.com/fivetaku/gptaku_plugins.git
```

### 2. 설치

```
/plugin install dd
```

### 3. Claude Code 재시작

(새 명령은 재시작해야 등록됩니다.)

### 4. 실행

뭔가 복사하거나 스크린샷을 찍은 뒤:

```
/dd 이런 느낌으로 만들어줘
/dd 왜 깨져?
/dd            (요청 없이 — dd가 클립보드를 읽고 대화 맥락을 이어 처리)
```

한글 입력 상태에서 `/dd`를 그대로 치면 `/ㅇㅇ`가 됩니다. 똑같이 동작해요. 한영 전환 안 해도 됩니다.

---

## 왜 dd인가

- **터미널 Claude Code는 클립보드 이미지를 붙여넣지 못합니다.** 원래는 스크린샷을 파일로 저장하고, 경로를 찾아서 "이 파일 봐줘" 해야 하죠. `dd`가 그걸 자동화합니다: 클립보드 이미지 → 자동 저장 → Claude가 봄.
- **보일러플레이트 제거.** "방금 복사한 에러", "위 레퍼런스 이미지" 같은 설명을 더는 안 써요. 그냥 `/dd` + 요청.
- **깔끔한 컨텍스트.** 원문 전체는 `~/dd/`에 남고, 대화엔 manifest와 (민감정보 마스킹된) 짧은 preview만 들어갑니다.
- **엉뚱한 클립보드 방지.** 클립보드엔 시각이 없어서, `dd`는 무엇을 잡았는지 보여주고 요청과 동떨어져 보이면 작업 전에 확인합니다.

---

## 작동 방식

```
복사 / 스크린샷
   → /dd [요청]
   → dd_clipboard.py가 현재 클립보드를 ~/dd/<날짜>/<id>/ 에 캡처
       · 텍스트 → content.txt   이미지 → image.png   + manifest.json
   → Claude는 manifest 요약만 읽음 (원문 전체 X)
   → 뭘 잡았는지 보여주고, 이상하면 확인, 맞으면 바로 작업
```

OS 클립보드는 가장 최근 복사 1개만 담습니다(이력 없음). `dd`는 항상 그 현재 항목을 잡고, preview에서 민감정보를 가리며, 7일 지난 캡처는 자동 정리합니다.

---

## 주요 기능

| 기능 | 설명 |
|------|------|
| 텍스트·이미지 캡처 | macOS / Windows / WSL / Linux 클립보드, 텍스트 또는 이미지 |
| instruction 패턴 주입 | 매 `/dd`마다 `${CLAUDE_PLUGIN_ROOT}`로 캡처 — 누구나 동작 |
| 확인 게이트 | 클립보드가 요청과 무관하거나 애매할 때만 작업 전 확인 |
| 지연 읽기 | `size_class`별로 읽어 거대한 붙여넣기가 컨텍스트를 안 덮침 |
| 민감정보 마스킹 | `api_key`, `Bearer`, `sk-`, `ghp_`, `xoxb-` 등 preview에서 가림 |
| 자동 정리 | `DD_RETENTION_DAYS`(기본 7일) 지난 캡처를 실행 시 삭제 |

---

## 명령어

| 명령어 | 설명 |
|--------|------|
| `/dd [요청]` | 현재 클립보드를 캡처해 작업 |
| `/ㅇㅇ [요청]` | `/dd`와 동일 (한글 입력 별칭) |

### 자연어 트리거

- "방금 캡처한 거 분석해줘", "이 레퍼런스로 만들어줘", "스크린샷 드롭"
- "drop clipboard", "use what I copied", "this screenshot"

---

## 요구사항

- [Claude Code](https://docs.anthropic.com/claude-code) CLI
- Python 3 (표준 라이브러리만)
- **macOS**: 바로 동작 (`pbpaste` / `osascript`)
- **Windows / WSL**: PowerShell(기본 내장) — 사용자 검증
- **Linux**: 클립보드 도구 설치(`xclip` / `wl-clipboard`) + GUI 세션 필요

---

## 라이선스

MIT

---

<div align="center">

**복사하고. `/dd` 하고. 끝.**

</div>
